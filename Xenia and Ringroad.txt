#include<bits/stdc++.h>
using namespace std;
#define ll long long 
int main(){
	ll n, m; cin>>n>>m;
	ll cnt=0, init=1;
	for(int i=0; i<m; i++) {
	    int loc; cin>>loc;
	    if(loc>=init) cnt+= loc-init;
	    else cnt+= n-(init-loc);
	    init = loc;
	}
	cout<<cnt;	
}
